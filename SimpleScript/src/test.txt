include("@/datetime/ISOString")
include("@/datetime/parseTime")

stopwatch = start()

ISOString(parseTime(local()))

echo string(stop(stopwatch)) + "\n"

/*
include("socket")

connection = TCPClient(localhost, 8080)

send(connection, "/api/getSongs")

res = recv(connection)

echo res + "\n"

closeTCP(connection)
